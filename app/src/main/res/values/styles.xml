<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorMainPrimary</item>
        <item name="colorPrimaryDark">@color/colorMainPrimaryDark</item>
        <item name="colorAccent">@color/colorMainAccent</item>
    </style>
    <style name="DarkActivityTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorMainPrimary</item>
        <item name="colorPrimaryDark">@color/colorMainPrimaryDark</item>
        <item name="colorAccent">@color/colorMainAccent</item>
    </style>

    <style name="LockActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
